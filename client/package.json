{
  "name": "myapp-frontend",
  "version": "1.0.0",
  "description": "Frontend application for myapp",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.js dist/index.js && cp src/components/Auth.js dist/Auth.js && cp public/index.html dist/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
},
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "webpack": "^5.89.0"
  }
}